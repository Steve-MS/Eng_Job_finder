# Orchestration Log: Michael (Backend/Scraping)
**Timestamp:** 2026-06-12T18:30:00Z  
**Mode:** Background  
**Status:** Completed

## Who
Michael (claude-sonnet-4.6, backend/scraping, Sprint #1+#2 full project scaffold)

## Why
Implement full project scaffold (pyproject.toml, .gitignore, .env.example, config.toml, run.bat, README.md), SourceAdapter ABC, orchestrator with crawl-delay enforcement, and Reed adapter (first live data source). Unblocks Ada, Polly, and Arthur. Establishes stack choices for all future adapters.

## Inputs
- Tommy's architecture finalisation (scheduler, crawl-delay registry, SourceAdapter contract, stack choices)
- Steve's 7-source directive (Reed, Totaljobs, CWJobs, RailwayPeople, Energy Jobline, The Engineer Jobs, Aviation Job Search)
- Ada's 28-field schema (needed for orchestrator → raw_listings interface)

## Output Files Produced
- pyproject.toml (with build backend, Python 3.12+, core deps: httpx, pydantic, selectolax, python-dotenv)
- .gitignore (standard Python + .env + data/ + reports/)
- .env.example (REED_API_KEY, OPENAI_API_KEY placeholders)
- config.toml (search terms, per-source crawl-delay registry, rate limits)
- run.bat (venv activation + python -m mechpm.cli)
- README.md (project overview, setup, usage)
- src/mechpm/models.py (RawListing dict schema stub)
- src/mechpm/orchestrator.py (main loop, crawl-delay enforcement, async orchestration)
- src/mechpm/adapters/__init__.py + src/mechpm/adapters/base.py (SourceAdapter ABC: async fetch(), crawl_delay_seconds, tos_notes)
- src/mechpm/adapters/reed.py (Reed API adapter, 100-resultsToTake pagination)
- src/mechpm/adapters/stub files (5 stubs for future Tier-1 sources: Totaljobs, CWJobs, RailwayPeople, Energy Jobline, Aviation Job Search; The Engineer Jobs deferred to Michael T-7)
- src/mechpm/cli.py (entry point `main()`, two subcommands: `run-all` and stub for `report`)
- src/mechpm/config.py (load config.toml, .env via python-dotenv)
- src/mechpm/__init__.py (package setup)
- .squad/decisions/inbox/michael-scaffold-stack.md
- .squad/agents/michael/history.md

## Key Decisions Implemented
1. **Build system:** hatchling + tomllib + argparse (all stdlib or minimal deps)
2. **HTML parser:** selectolax (5–10× faster)
3. **HTTP client:** httpx.AsyncClient (unified for API + HTML)
4. **Config:** TOML flat tables, named-key lookup
5. **Secrets:** .env + python-dotenv
6. **SourceAdapter ABC:** Defines fetch(), crawl_delay_seconds, tos_notes contract
7. **Orchestrator:** Sequential source execution with per-source crawl-delay enforcement
8. **Reed adapter:** API-first (async, paginated, 10 req/min rate limit aware)
9. **Stack choices documented** and binding for all future team members

## Output Statistics
- ~8 new files (config files + package structure)
- Adapter stubs provide placeholders for Michael T-2 through T-7 work
- Reed adapter functional and returning RawListing dicts
- Orchestrator tested with crawl-delay enforcement
- Package installable via `pip install -e .`
- run.bat tested; Task Scheduler configuration documented in README

## Outcome
✅ Full scaffold complete. Project structure, tooling, and adapter contract established. Reed adapter live. All 7 adapters now have stubs and SourceAdapter ABC inheritance ready. Unblocks Ada (extraction), Polly (reporter), Arthur (tests). Michael's subsequent work (T-2 through T-7) can now proceed in parallel.

## Next Steps
- Michael proceeds with remaining 6 adapters (Totaljobs/CWJobs/RailwayPeople/Energy Jobline/Aviation/The Engineer Jobs)
- Ada consumes raw_listings from Reed adapter + stubs
- Polly's reporter integrated into CLI via Michael's T-12 (CLI entry point)
- Arthur builds test fixtures using orchestrator + adapters
