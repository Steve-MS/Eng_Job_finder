# Orchestration Log: Tommy (Lead Architect)

**When:** 2026-06-12T16:30:00Z UTC  
**Agent:** Tommy (claude-opus-4.6)  
**Mode:** Background (368s)  
**Task:** MVP architecture proposal — tech stack, pipeline design, adapter contract, repo layout, scope cut

## Outcome
**Status:** ✅ Complete

**Deliverable:** `.squad/decisions/inbox/tommy-mvp-architecture.md`

**What was delivered:**
- Tech stack decision: Python 3.12+, SQLite (single-file `data/mechpm.db`), OpenAI GPT-4o-mini, httpx async, Markdown reports
- Pipeline architecture: Scheduler → Orchestrator → Fetch (adapters) → raw_listings → Extract (LLM) → normalized_listings → Dedup + Filter → Report
- Source-Adapter ABC contract: async fetch(), rate_limit property, tos_notes, never crashes (graceful error logging)
- Repo layout: `src/mechpm/{orchestrator, models, adapters/, extractor/, dedup/, storage/, reporter/}`, `data/mechpm.db`, `reports/`, `config.toml`, `tests/`
- Module ownership: Tommy (design), Michael (orchestrator/CLI/adapters), Ada (extractor/dedup), Polly (reporter), Arthur (tests/acceptance)
- **MVP scope IN:** Reed API + CWJobs adapters, 7-field LLM extraction, SQLite storage, basic Jaro-Winkler dedup (>0.85), Markdown report, CLI entry, manual trigger
- **MVP scope DEFERRED:** LinkedIn/Indeed, Task Scheduler, HTML/email delivery, historical trends, confidence scoring, browser adapters (v0.2+)

**Why this matters:**
Locks in MVP tech stack and pipeline shape. Confirms Windows-native local execution (no cloud infra in v0.1). Establishes module ownership and adapter contract that Michael + Ada depend on.

## Files Produced
- `.squad/decisions/inbox/tommy-mvp-architecture.md` (~8200 bytes)
- `.squad/agents/tommy/history.md` (appended)

## Next Steps
Awaits Steve approval. Michael will implement orchestrator/CLI per this contract. Ada will implement LLM extraction interface. Arthur will define acceptance criteria per pipeline stages.
